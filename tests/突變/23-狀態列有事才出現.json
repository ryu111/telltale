[
  {
    "label": "status row always drawn",
    "file": "plugins/telltale/hooks/layout.ts",
    "old": "  if (first.dropped.length === 0) {\n    return { ...first, status: false };\n  }",
    "new": "  if (first.dropped.length === 0) {\n    return { ...first, status: true };\n  }"
  },
  {
    "label": "dropped panels do not bring the status row back",
    "file": "plugins/telltale/hooks/layout.ts",
    "old": "  const second = computeWithFixed(panels, maxRows, FIXED_ROWS);\n  return { ...second, status: true };",
    "new": "  return { ...first, status: false };"
  },
  {
    "label": "error does not force the status row",
    "file": "plugins/telltale/hooks/layout.ts",
    "old": "  if (opts?.status === true) {\n    const r = computeWithFixed(panels, maxRows, FIXED_ROWS);\n    return { ...r, status: true };\n  }",
    "new": "  if (opts?.status === true && false) {\n    const r = computeWithFixed(panels, maxRows, FIXED_ROWS);\n    return { ...r, status: true };\n  }"
  }
]
