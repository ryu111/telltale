# 09-發佈：README（貼 validate 輸出）、LICENSE、CI

- 對應 SDD 節次：切片層 #9；§2.5；題目 §6 第 3 條
- 可碰檔案：`README.md` `LICENSE` `.github/workflows/check.yml`
- 相關檔案：`docs/SDD.md`（§0 衝突表、§1.2 預設高度、§1.5 死區、§1.6 command 表、§2.1 store、§2.5）、`docs/題目.md`（§2 為什麼一個 plugin 多個面板、§6）、`docs/tasks/band/00-共同規則.md`
- 驗收測試：`tests/單元/test_readme.py`
- 完成條件：`make check` 全綠；驗收測試由紅轉綠；code-review Spec 軸零缺漏、零超範圍
- 依賴（Blocked by）：06, 07
- 並行度：單獨跑

## 題目
README.md（英文、給全世界）要有：一句話是什麼、一張畫面（文字貼圖）、安裝（`claude plugin install` 或 `--plugin-dir`，需要 `CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1`）、`/telltale` 用法表、**一段 ```` ```text ```` 區塊逐字貼 `claude plugin validate --strict .` 的 `hooks:` 與 `calls:` 兩行**（標題 `## What this plugin can touch`）、設計取捨（為什麼一個 plugin 多個面板、第三方只能發 PR）、已知限制（預設 4 列、最右 4 欄死區、emoji 寬度不保證、<20 欄降級、開關全 session 共用、store 檔位置 `~/.claude/plugins/store/` 與清除指令）。LICENSE：MIT，`Copyright (c) 2026 ryu111`。CI：`.github/workflows/check.yml` 裝 uv、bun（`oven-sh/setup-bun@v2`）、`npm install -g @anthropic-ai/claude-code`，跑 `make check` 與 `make mutate`。
