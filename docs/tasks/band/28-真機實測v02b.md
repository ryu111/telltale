# 28-真機實測 v0.2b（手動，主 agent 自己做）：按鍵、tool 次數、session 隔離、換邊

- 對應 SDD 節次：§2.8 全部；/拆任務 規則 4b（每輪末張真機實測票）
- 可碰檔案：`docs/實測/v02b.md`（新）、`docs/實測/寬度.md`（重跑表）、`.claude/CLAUDE.md`（坑）；發現缺陷 → 開補票，不在這票改 code
- 相關檔案：`docs/實測/agents.md`（tmux／SGR 送法）、`scripts/量寬度.py`
- 驗收測試：`docs/實測/v02b.md` 的畫面與數字
- 完成條件：下列每項有 tmux capture 或 store 內容佐證；`grep -iE "does not validate|hook failed|refused" /tmp/tt28.log` 0 行
- 依賴（Blocked by）：27
- 並行度：單獨跑

## 要驗
1. 帶子 agents 標題列右側看得到 `[1 2 3] [S C F] [R B RB] [x]`，目前值粗體；SGR 點 `2` → cell 變 v2、`/telltale agents style` 報 `v2`；點 `S` → 只剩標題列；點 `x` 清掉一個 failed cell。
2. cell 標題列有 ` · N tools`，數字跟該輪工具呼叫數一致（派一個 Explore subagent 對照它的 toolUses）。
3. `claude -p "say hi" --plugin-dir plugins/telltale`（headless）跑完後：store 檔沒有新增 `agents.cells.<那個 sid>`、`error.agents.*` 沒有出現 `not available in this mode`。
4. 終端機 REPL 的 store 鍵是 `agents.cells.<本 session id>`；同時開第二個終端機 session，各自只看到自己的 cell；關掉一個 24 h 內另一個不清它的鍵。
5. 點 `B` → Pane 關閉、帶子只在輸入框上方；點 `R` → Pane 回來、上方沒有第二條；點 `RB` → Pane 只有 agents、上方只有其他面板（`TELLTALE_DEV=1` 開 hello 才看得到）。
6. 寬度 200→15 重跑 `scripts/量寬度.py`，三種型態各至少一個寬度 ok；狀態列仍然沒事不出現。
