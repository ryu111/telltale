# 04-面板：Panel 型別、hello、clock、註冊表（純函式）

- 對應 SDD 節次：§1.1、§2.2、§2.2a、§2.3；不變量 I6、I10
- 可碰檔案：`hooks/panel.ts` `hooks/panels/hello.ts` `hooks/panels/clock.ts` `hooks/panels/index.ts` `hooks/mutations.json`
- 相關檔案：`docs/SDD.md`（§1.1、§2.2、§2.2a、§2.3）、`hooks/width.ts`（用 `fit`／`displayWidth` 裁行）、`docs/tasks/band/00-共同規則.md`
- 驗收測試：`hooks/panels.test.ts`
- 完成條件：`make check` 全綠；驗收測試由紅轉綠；`make mutate` 對本票兩條突變全紅；code-review Spec 軸零缺漏、零超範圍
- 依賴（Blocked by）：02
- 並行度：可與 01、03 同時（02 merge 後）

## 題目
`hooks/panel.ts` 匯出 SDD §1.1 的 `Tone`、`PanelLine`、`PanelView`、`PanelIo`、`Panel<D>`。`hooks/panels/hello.ts` 匯出 `hello: Panel<{ tick: number }>`、`hooks/panels/clock.ts` 匯出 `clock: Panel<{ now: number }>`，內容照 §2.3／§2.2a（時間用本機時區 `HH:MM:SS`）。`hooks/panels/index.ts` 匯出 `PANELS: readonly Panel[] = [hello, clock]`。每行都先過 `fit(text, columns)`；`lines.length ≤ rows`。不 import `claude-code`、不碰 `$`。

突變：
1. label `hello: placeholder missing when data is undefined` — `data === undefined` 時回空 `lines`。
2. label `panels: line not fitted to columns` — 拿掉 hello 第 2 行的 `fit`。
