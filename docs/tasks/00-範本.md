# 00-範本：＿＿＿（複製後改成 NN-<slug>.md）

- 對應 SDD 節次：切片層 #＿
- 可碰檔案：`src/專案/＿.py`（範圍 hook 讀這行的反引號，實作只准動這些；tests/ 是出題者的）
- 相關檔案：`docs/SDD.md`（實作者除了可碰檔案外只准讀這些；省 token 的主桿，可留空）
- 驗收測試：`tests/流程/test_＿.py`（先寫好、先看它紅，再派實作）
- 完成條件：`make check` 全綠；驗收測試由紅轉綠；`mattpocock-skills:code-review` 以本檔為 spec，Spec 軸零缺漏、零超範圍
- 依賴（Blocked by）：無
- 並行度：可與 TASK-＿ 同時（碰的檔案不交集）

## 題目
（給 X 要得到 Y，一句話。細節在驗收測試裡，不在這裡）

## 做這張票的順序
機械化版：`make ticket T=docs/tasks/<本檔>`（`scripts/跑票.py`）走完 1–5，停下來就出報告。手動版如下。

實作者（角色定義在 `AGENTS.md`；codex／agy 走 `/委派 --票`，Claude 的 sonnet subagent 只在兩家都不行時上）：
1. 只有 Claude subagent 做這步：`echo docs/tasks/<本檔> > docs/tasks/目前`，範圍 hook 從此只放行可碰檔案。codex／agy 不寫指針（會留下範圍外的 diff 被 revert），靠跑完 diff 對照。
2. 先看驗收測試是紅的，實作到 `make check` 綠。
3. Claude subagent：`rm docs/tasks/目前`。commit，回報數字。

出題者（主 agent 或 跑票.py）：
4. diff 對照可碰檔案；審查者角色（另一家 CLI）以本檔為 spec 做 Spec 軸：零缺漏、零超範圍。
5. `make mutate` 過門檻，merge。紅了回出題，不回實作。
