# v0.2b 真機實測（票 28；2026-09-18；claude 2.1.274；tmux 150×40；`--plugin-dir plugins/telltale --debug-file /tmp/tt28.log`）

對應 `docs/SDD.md` §2.8（票 24–27）。SGR 送法同 `docs/實測/agents.md`。

## 1. 標題列按鍵（票 24／27）
Pane dock 右側，第 2 列：
```
│─ agents ─────────────────────────[1 2 3] [S C F] [R B RB] [x]────
```
| 動作（SGR 點） | 結果 |
|---|---|
| `2`（欄 122） | store `style.agents = v2`；`/telltale agents style` → `agents style: v2` |
| `S`（欄 128） | 面板只剩標題列；`/telltale agents size` → `agents size: summary` |
| `F`（欄 132） | `size.agents = full`，cell 展開 |
| `B`（欄 138） | Pane 關掉、帶子落到輸入框上方，標題列同樣帶按鍵（欄 118 起）；`edge.agents = bottom` |
| `R`（輸入框上方帶子欄 137、列 31） | Pane 回來、上方沒有第二條帶子；`edge.agents = right` |
| `RB`（欄 141） | Pane 只有 agents；上方沒有其他面板（本 session 沒開 `TELLTALE_DEV`）；`edge.agents = both` |
| `x` | 沒有 failed cell 可點，只靠 `tests/hooks/buttons.test.tsx` 的 clear 測試 |

粗體在 capture 看不出來，靠 `buttons.test.tsx` 的 active 斷言。

## 2. tool 次數（票 25）
subagent cell（steps `prompt, Bash, reply`）標題列：
```
│┌ ✓ sub 1s · 1 tools ·                                           ┐
```
main cell（`只回 ok`，steps `prompt, think, reply`）沒有那段（0 tools 不顯示）；完成後折成 `✓ 1 turns · 只回 ok`。

## 3. session 隔離（票 26）
- `claude -p "say hi" --plugin-dir …`（headless）：debug log 有 `hooks module telltale loaded`，跑完 store 檔**沒有**新增 `agents.cells.<sid>`，`error.agents.*` 沒有 `not available in this mode`（grep 0 筆）。
- 兩個終端機 session 同時開：store 鍵 `agents.cells.19a56e45-…` 與 `agents.cells.a3071f27-…` 各一份，互不混。
- 舊的裸鍵 `agents.cells`（v0.2 留下的）沒人讀也沒人清，留在 store 檔裡無害；`~/.claude/plugins/store/telltale_skills-dir-*.json` 裡 v0.2 寫的 `error.agents` 同理。

## 4. 寬度重跑（`OUT=/tmp/tt28-width scripts/量寬度.sh tt28b`）
```
w=200 rows=27 widest=200 content=True 型態=dock ok
w=150 rows=27 widest=150 content=True 型態=dock ok
w=110 rows=27 widest=110 content=True 型態=dock ok
w=80 rows=4 widest=80 content=True 型態=inline ok
w=60 rows=4 widest=60 content=True 型態=inline ok
w=45 rows=4 widest=45 content=True 型態=inline ok
w=30 rows=4 widest=30 content=True 型態=inline ok
w=15 rows=3 widest=15 content=False 型態=inline ok
```
退出碼 0。`edge = right`／`both` 時 AbovePrompt 讓位，寬終端不再出現 v0.2 那條重複的裸帶子（`docs/實測/寬度.md` 第 41 行的現象消失）。

## 5. hook 錯誤
`grep -ciE "does not validate|hook failed|refused"` 對 `/tmp/tt28.log`、`/tmp/tt28b.log`、`/tmp/tt28-headless.log` 都是 0。

## 沒驗到
- `x` 清除按鍵的真機點擊（沒有 failed cell）。
- `both` 時上方畫「其他面板」（要 `TELLTALE_DEV=1`；只有 `tests/hooks/edge.test.tsx` 守）。
- 24 h 舊 session 清除（只有 `headless.test.tsx` 的假時鐘守）。
