# 實測：寬度 200 → 15（票 08，DoD #4）

2026-09-17，Claude Code 2.1.267，tmux 34 列，`scripts/量寬度.sh tt`（量法：`scripts/量寬度.py`，東亞寬字算 2）。

| 欄數 | 帶子列數 | 最寬列 | 有面板內容 | 結果 |
|---|---|---|---|---|
| 200 | 7 | 200 | 有 | ok |
| 150 | 7 | 150 | 有 | ok |
| 110 | 7 | 110 | 有 | ok |
| 80 | 7 | 80 | 有 | ok |
| 60 | 7 | 60 | 有 | ok |
| 45 | 7 | 45 | 有 | ok |
| 30 | 7 | 30 | 有 | ok |
| 15 | 1 | 12 | 無（< MIN_COLUMNS 單列降級：`telltale · 2`＋引擎的 `[-]`） | ok |

- 每一步最寬列 = 欄數：面板標題列 `─ label ───` 補滿到 columns，沒有任何一列超寬、沒有換行。
- 高度 7 列在所有寬度都不變（§1.2 規則 7：layout 不吃 columns）。
- debug log 全程無 `does not validate|hook failed|refused`。
- 沒有自動測試守這一條：它要真的開 claude；每次改 band.tsx／width.ts 後重跑腳本。

## 票 18：v0.2 重跑（2026-09-18，Claude Code 2.1.274，agents 面板開著）

2026-09-18，agents 面板預設開著（DESIGN §6 落地）之後畫面內容變了（多一塊 Pane），照票 18 指示重新跑一次
`scripts/量寬度.sh`：`tmux new-session -x 150 -y 34`，`TELLTALE_DEV=1 CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1
claude --plugin-dir plugins/telltale --debug-file /tmp/tt18.log`（三個面板 agents／hello／clock 都開），等
`hooks module telltale loaded` 出現後跑 `scripts/量寬度.sh tt18`。

| 欄數 | 帶子列數 | 最寬列 | 有面板內容 | 結果 |
|---|---|---|---|---|
| 200 | 17 | 200 | 有 | ok |
| 150 | 17 | 150 | 有 | ok |
| 110 | 17 | 110 | 有 | ok |
| 80 | — | — | — | **FAIL：band not found** |
| 60 | — | — | — | **FAIL：band not found** |
| 45 | — | — | — | **FAIL：band not found** |
| 30 | — | — | — | **FAIL：band not found** |
| 15 | 1 | 12 | 無（< MIN_COLUMNS 單列降級） | ok |

**不是腳本假設面板數的問題，沒有改腳本**：票 18 只授權在「腳本假設只有 hello/clock 兩個面板而跑不動」時才動
`scripts/量寬度.sh`；這次的 FAIL 原因是另一件事——2.1.274 的 `Pane`（`agents` 面板現在走 `Pane`，不是純
`AbovePrompt`）在 ≥ 110 欄 dock 到終端右側時，畫面下半部仍有一行不帶邊框、貼齊欄 0 的 `telltale · N panels`
（`AbovePrompt` 自己的狀態行，跟 dock 的 `Pane` 是兩個獨立 render），腳本抓的就是這一行；但在 80／60／45／30 欄，
`Pane` 換成貼在輸入框上方的**帶框**方框（`╭─╮`／`│ ... │`），這時 `AbovePrompt` 那行狀態被完全擠掉（票 16 實測
記錄過同一件事：「AbovePrompt 這時的高度預算被擠到只夠顯示狀態列，`agents` 面板本身被 dropped」——這裡是反過來，
連狀態列都沒有），畫面上再也沒有任何一行是「以 `telltale` 開頭、欄 0 起算、不帶前綴字元」的裸行，`量寬度.py` 的
`帶子()` 找不到起點，回報 `rows=0 FAIL (band not found)`（不是「超寬」，是「抓不到」）。

實際看 `/tmp/telltale-width/80.txt`，帶子內容其實都在，且沒有超寬：

```
╭──────────────────────────────────────────────────────────────────────────────╮
│ telltale · 3 panels                                                        ✕ │
│ ─ agents ─────────────────────────────────────────────────────────────────── │
│ ✓ main 23m51 · <task-notification>                                           │
│ <task-id>aa56f4d9082904ace</task-id>                                         │
│ <to                                                                          │
│ ─ hello ──────────────────────────────────────────────────────────────────── │
│ hello · 00:23:58                                                             │
│ ─ clock ──────────────────────────────────────────────────────────────────── │
│ 00:24:00                                                                     │
╰──────────────────────────────────────────────────────────────────────────────╯
```

框線本身寬度＝80＝欄數，沒有任何一列超寬、沒有換行——**畫面是對的，錯的是腳本的偵測條件**（它只認裸行，不認
Pane 的框線變體）。這屬於票 18 完成條件裡「把結果貼進去」的部分，不屬於「腳本假設兩個面板」那個例外，所以照票
指示**不動腳本**，如實記下：v0.2 起，寬度腳本在 Pane 落到「輸入框上方帶框」這個型態時量不到，這件事本身該回收
成腳本的已知限制或另開票補偵測條件（不在本票可碰檔案範圍）。

## 票 06 的手動實測（同一個 session，DoD #3、I7）

- 開兩個面板 → SGR 點 clock 標題列（列 24、欄 10）→ clock 消失、hello 的秒數照走、`hooks module telltale loaded` 仍只有 1 行（沒重載）。
- 點 hello 標題列最右 4 欄（欄 148）→ 沒反應（死區）。
- 點 hello 標題列 → 只剩一列 `telltale · all panels off · /telltale on`。
- `/telltale status` → `○ hello  off` / `○ clock  off` / `band: 1 rows of 9 available`；`/telltale on` → 兩行 `off → on`，帶子回到 7 列。
